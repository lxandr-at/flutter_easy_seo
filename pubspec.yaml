name: flutter_easy_seo
description: Generates SEO HTML from Flutter widget trees for search engine bots. FREE for individuals and small businesses (≤ 5 employees, < $1M revenue). Commercial license required for enterprises.
version: 1.0.0
homepage: https://github.com/yourusername/flutter_easy_seo

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  meta: ^1.18.0
  path: ^1.9.1
  web: ^1.1.1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter: